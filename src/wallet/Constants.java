package wallet;
public class Constants {

    public enum AuthResult {
        AUTH_ERROR,
        ADMINISTRATOR,
        USER
    }
    
    public enum WalletMode {
        EXPENSES,
        BUDGET
    }
    
}
